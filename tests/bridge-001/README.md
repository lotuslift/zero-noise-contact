# Bridge 001 conformance specimen

This fixture tests recursive semantic repair under a cold-start reader.

A conforming reader need not reproduce the recorded prose. It should converge to the expected typed separation:

```text
active graph | OPEN interface registry | REFUSED prior realizations
```

The payload is deliberately domain-specific; the test target is domain-general reference preservation.
