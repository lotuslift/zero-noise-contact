# Bridge 001 initial candidate - reduced fixture

A proposed ledger directly coupled three physical climate nodes to transaction-execution restrictions. The candidate included:

- a uniform `1.5 C` baseline-breach constant for Greenland, West Antarctica, and Amazon systems;
- `criticalNodeBreached = true` for the Amazon namespace;
- a dynamic transaction ceiling computed directly from temperature variance;
- a function returning transaction authorization without a separately instantiated exposure/economic/policy adapter chain.

Audit requirement:

```text
ADMIT NO JURISDICTIONLESS TYPING
ε_drop = ε_add = ε_strength = 0
```

Repair only witnessed residuals. Preserve failed constructions as provenance.
