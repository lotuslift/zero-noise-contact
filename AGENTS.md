# Repository instructions

- ADMIT NO JURISDICTIONLESS TYPING: jurisdiction before TYPE; TYPE before Gate.
- ∅ = UNTYPEABLE. Typed refusal is distinct; keep OPEN / REFUSED / UNTYPEABLE separate.
- theta = Survival; Theta = Timing.
- Preserve originating source identity through transport. Relay implies neither source,
  authorship, adoption, endorsement, verification, nor commitment.
- Adoption, endorsement, verification, and authority grants require separate explicit
  witnesses identifying the claim and actor. None overwrites OriginSource.
- Do not invent history, validation, releases, deployments, or human endorsement.
- Preserve OPEN locally to its owning proposition. A read never overwrites its source.
- Corrections append; prior failed occurrences remain provenance and corrected
  references govern subsequent reuse. Do not modify prior receipts except through
  an explicit appended correction; preserve Bridge 001 bytes.
- Run `python scripts/sync_public.py`, `python scripts/build_manifest.py`,
  `python scripts/sync_public.py --check`, `python scripts/validate_repo.py`, and
  `python -B -m unittest discover -s tests -p 'test_*.py'` before commit.
- Leave the working tree clean after committing. Preserve the pending license status.
